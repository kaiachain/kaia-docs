# 単位

単位変換のために、Kaia-SDKは`kaia`、`Gkei`、`kei`の変換ユーティルをサポートしている。 対応ユニット](https://docs.kaia.io/learn/kaia-native-token/#units-of-kaia-)を参照。

<CH.Spotlight>
  <CH.Code>
    ```js unitUtils.js
    const {
      formatKlay,
      parseKlay,
      formatKlayUnits,
    } = require('@kaiachain/web3js-ext')

    async function main() {
      console.log(
        'example basefee in ston =',
        formatKlayUnits('0x5d21dba00', 'ston')
      )
      console.log('transfer amount in klay =', formatKlay('1230000000000000000'))
      console.log(
        'example gas price in peb =',
        parseKlayUnits('50', 'ston').toString()
      )
      console.log('transfer amount in peb =', parseKlay('9.87').toString())
    }

    main()
    ```

    ---

    ```zsh output
    ❯ node unitUtils.js
    example basefee in ston = 25.0
    transfer amount in klay = 1.23
    example gas price in peb = 50000000000
    transfer amount in peb = 9870000000000000000
    ```
  </CH.Code>

  ---

  `formatKlay`、`parseKlay`、`formatKlayUnits` を **@kaiachain/web3js-ext** パッケージからインポートする。

  ```js unitUtils.js focus=1
  ```

  ---

  **peb**を他の単位に変換するには、`formatKlayUnits`を使用する。 例えば、`formatKlayUnits` を使って、**peb** を **ston** に変換する。

  ```js unitUtils.js focus=8:11
  ```

  ---

  `formatKlay`を使用すると、**peb**は**klay**に変換される。

  ```js unitUtils.js focus=12
  ```

  ---

  `parseKlayUnits`を使用して、値を1つの単位から**peb**に変換する。 最初のparamは**value**、2番目のparamは**変換する単位**である。

  ```js unitUtils.js focus=13:16
  ```

  ---

  `parseKlay`を使用して**klay**を**peb**に変換する。

  ```js unitUtils.js focus=17
  ```
</CH.Spotlight>
